// // src/tests/setup.ts
// import { config } from 'dotenv';
// import path from 'path';

// config({
//     path: path.resolve(process.cwd(), '.env.test'), // pakai path.resolve + process.cwd(), JANGAN cuma string relatif
//     override: true,
// });

// console.log('SETUP.TS loaded, DATABASE_URL:', process.env.DATABASE_URL); // tambahin ini juga